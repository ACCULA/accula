import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles(() => ({
  root: {
    overflow: 'hidden'
  },
  content: {
    overflow: 'hidden'
  },
  formInput: {
    padding: '15px 20px',
    marginBottom: '15px'
  }
}))
