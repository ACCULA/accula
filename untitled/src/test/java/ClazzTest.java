import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class ClazzTest {
    @Test
    public void testS() {
        assertEquals("", new Clazz().s());
    }
}